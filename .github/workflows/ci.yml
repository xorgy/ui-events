env:
  # We aim to always test with the latest stable Rust toolchain, however we pin to a specific
  # version like 1.70. Note that we only specify MAJOR.MINOR and not PATCH so that bugfixes still
  # come automatically. If the version specified here is no longer the latest stable version,
  # then please feel free to submit a PR that adjusts it along with the potential clippy fixes.
  RUST_STABLE_VER: "1.92" # In quotes because otherwise (e.g.) 1.70 would be interpreted as 1.7
  # The purpose of checking with the minimum supported Rust toolchain is to detect its staleness.
  # If the compilation fails, then the version specified here needs to be bumped up to reality.
  # Be sure to also update the rust-version property in the workspace Cargo.toml file,
  # plus all the README.md files of the affected packages.
  RUST_MIN_VER: "1.85"
  # This crate has a different MSRV for no_std builds.
  RUST_MIN_NO_STD_VER: "1.85"
  # List of packages that will be checked with the minimum supported Rust version.
  # This should be limited to packages that are intended for publishing.
  RUST_MIN_VER_PKGS: "-p ui-events -p ui-events-apple-common -p ui-events-appkit -p ui-events-uikit -p ui-events-wayland -p ui-events-web -p ui-events-winit -p ui-events-x11 -p ui-events-xkb -p ui-input-state -p ui-theme"
  # List of packages that will be checked for `no_std` builds.
  # This should be limited to packages that are intended for publishing.
  RUST_NO_STD_PKGS: "-p ui-events -p ui-events-apple-common -p ui-events-appkit -p ui-events-uikit -p ui-events-wayland -p ui-events-x11 -p ui-events-xkb -p ui-input-state -p ui-theme"
  # List of features that depend on the standard library and will be excluded from no_std checks.
  # ui-events-wayland's `xkb` reads the keymap fd through `std::fs`, so it implies `std`.
  FEATURES_DEPENDING_ON_STD: "std,default,xkb"
  # The Wayland and X11 adapters target Linux/Unix, and both reach the `xkb`
  # feature's logical-key resolution through `ui-events-xkb`, which links the
  # system libxkbcommon (via `xkbcommon`). That C dependency only builds on the
  # Linux runners (where CI installs it), so these three crates are excluded from
  # the wasm and cross-compiled jobs and tested with all features on Linux only.
  XKB_LINUX_ONLY_EXCLUDE: "--exclude ui-events-wayland --exclude ui-events-x11 --exclude ui-events-xkb"


# Rationale
#
# We don't run clippy with --all-targets because then even --lib and --bins are compiled with
# dev dependencies enabled, which does not match how they would be compiled by users.
# A dev dependency might enable a feature that we need for a regular dependency,
# and checking with --all-targets would not find our feature requirements lacking.
# This problem still applies to cargo resolver version 2.
# Thus we split all the targets into two steps, one with --lib --bins
# and another with --tests --benches --examples.
# Also, we can't give --lib --bins explicitly because then cargo will error on binary-only packages.
# Luckily the default behavior of cargo with no explicit targets is the same but without the error.
#
# We use cargo-hack for a similar reason. Cargo's --workspace will do feature unification across
# the whole workspace. While cargo-hack will instead check each workspace package separately.
#
# Using cargo-hack also allows us to more easily test the feature matrix of our packages.
# We use --each-feature & --optional-deps which will run a separate check for every feature.
#
# We use cargo-nextest, which has a faster concurrency model for running tests.
# However cargo-nextest does not support running doc tests, so we also have a cargo test --doc step.
# For more information see https://github.com/nextest-rs/nextest/issues/16
#
# The MSRV jobs run only cargo check because different clippy versions can disagree on goals and
# running tests introduces dev dependencies which may require a higher MSRV than the bare package.
#
# For no_std checks we target x86_64-unknown-none, because this target doesn't support std
# and as such will error out if our dependency tree accidentally tries to use std.
# https://doc.rust-lang.org/stable/rustc/platform-support/x86_64-unknown-none.html
#
# We don't save caches in the merge-group cases, because those caches will never be re-used (apart
# from the very rare cases where there are multiple PRs in the merge queue).
# This is because GitHub doesn't share caches between merge queues and the main branch.

name: CI

on:
  pull_request:
  merge_group:
  # We run on push, even though the commit is the same as when we ran in merge_group.
  # This allows the cache to be primed.
  # See https://github.com/orgs/community/discussions/66430
  push:
    branches:
      - main

jobs:
  fmt:
    name: formatting
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install stable toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_STABLE_VER }}
          components: rustfmt

      - name: cargo fmt
        run: cargo fmt --all --check

      - name: Install Taplo
        uses: uncenter/setup-taplo@09968a8ae38d66ddd3d23802c44bf6122d7aa991 # v1
        with:
          version: "0.9.3"

      - name: Run taplo fmt
        run: taplo fmt --check --diff

      - name: install ripgrep
        run: |
          sudo apt update
          sudo apt install ripgrep

      - name: check copyright headers
        run: bash .github/copyright.sh

      # Pin to 1.5.1 due to issues with 2.x and intralinks / nightly builds.
      - name: install cargo-rdme
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-rdme@1.5.1

      - name: cargo rdme (ui-events)
        # Ideally, we'd set heading-base-level=0 in the config file for cargo rdme, but that only
        # supports being placed at the top level, which we want to avoid cluttering.
        run: cargo rdme --workspace-project=ui-events --heading-base-level=0 --check

      - name: cargo rdme (ui-events-apple-common)
        run: cargo rdme --workspace-project=ui-events-apple-common --heading-base-level=0 --check

      - name: cargo rdme (ui-events-appkit)
        run: cargo rdme --workspace-project=ui-events-appkit --heading-base-level=0 --check

      - name: cargo rdme (ui-events-uikit)
        run: cargo rdme --workspace-project=ui-events-uikit --heading-base-level=0 --check

      - name: cargo rdme (ui-events-wayland)
        run: cargo rdme --workspace-project=ui-events-wayland --heading-base-level=0 --check

      - name: cargo rdme (ui-events-web)
        run: cargo rdme --workspace-project=ui-events-web --heading-base-level=0 --check

      - name: cargo rdme (ui-events-winit)
        run: cargo rdme --workspace-project=ui-events-winit --heading-base-level=0 --check

      - name: cargo rdme (ui-events-x11)
        run: cargo rdme --workspace-project=ui-events-x11 --heading-base-level=0 --check

      - name: cargo rdme (ui-events-xkb)
        run: cargo rdme --workspace-project=ui-events-xkb --heading-base-level=0 --check

      - name: cargo rdme (ui-input-state)
        run: cargo rdme --workspace-project=ui-input-state --heading-base-level=0 --check

      - name: cargo rdme (ui-theme)
        run: cargo rdme --workspace-project=ui-theme --heading-base-level=0 --check

  clippy-stable:
    name: cargo clippy
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v4

      - name: install stable toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_STABLE_VER }}
          targets: x86_64-unknown-none
          components: clippy

      - name: install cargo-hack
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-hack

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: cargo clippy (no_std)
        run: cargo hack clippy ${{ env.RUST_NO_STD_PKGS }} --locked --optional-deps --each-feature --exclude-features ${{ env.FEATURES_DEPENDING_ON_STD }} --features libm --target x86_64-unknown-none -- -D warnings

      - name: cargo clippy
        if: runner.os == 'Linux'
        run: cargo hack clippy --workspace --locked --optional-deps --each-feature --ignore-unknown-features --features std -- -D warnings

      # The `xkb` feature's `xkbcommon` compiles only on unix, so its variants
      # are checked on the Linux runners only (see `XKB_LINUX_ONLY_EXCLUDE`).
      - name: cargo clippy (non-Linux)
        if: runner.os != 'Linux'
        run: cargo hack clippy --workspace --locked --optional-deps --each-feature --ignore-unknown-features --exclude-features xkb --features std -- -D warnings

      - name: cargo clippy (auxiliary)
        if: runner.os == 'Linux'
        run: cargo hack clippy --workspace --locked --optional-deps --each-feature --ignore-unknown-features --features std --tests --benches --examples -- -D warnings

      - name: cargo clippy (auxiliary, non-Linux)
        if: runner.os != 'Linux'
        run: cargo hack clippy --workspace --locked --optional-deps --each-feature --ignore-unknown-features --exclude-features xkb --features std --tests --benches --examples -- -D warnings

  clippy-stable-wasm:
    name: cargo clippy (wasm32)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install stable toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_STABLE_VER }}
          targets: wasm32-unknown-unknown
          components: clippy

      - name: install cargo-hack
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-hack

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: cargo clippy
        run: cargo hack clippy --workspace ${{ env.XKB_LINUX_ONLY_EXCLUDE }} --locked --target wasm32-unknown-unknown --optional-deps --each-feature --ignore-unknown-features --features std -- -D warnings

      - name: cargo clippy (auxiliary)
        run: cargo hack clippy --workspace ${{ env.XKB_LINUX_ONLY_EXCLUDE }} --locked --target wasm32-unknown-unknown --optional-deps --each-feature --ignore-unknown-features --features std --tests --benches --examples -- -D warnings

  apple-adapter-targets:
    name: apple adapter targets
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: install stable toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_STABLE_VER }}
          targets: aarch64-apple-ios
          components: clippy

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: cargo check (iOS)
        run: cargo check -p ui-events-uikit --locked --all-features --target aarch64-apple-ios

      - name: cargo clippy (iOS)
        run: cargo clippy -p ui-events-uikit --locked --all-features --target aarch64-apple-ios -- -D warnings

  test-stable:
    name: cargo test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v4

      - name: install stable toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_STABLE_VER }}

      - name: install cargo-nextest
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-nextest

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: install libxkbcommon (Linux)
        # The `xkb` feature of the Wayland and X11 adapters, enabled by
        # `--all-features`, links the system libxkbcommon; `xkb-data` lets their
        # keymap tests run instead of skipping. Only the Linux job builds and runs
        # this dependency, so only there are those adapters tested with all
        # features (see `XKB_LINUX_ONLY_EXCLUDE`).
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libxkbcommon-dev xkb-data

      - name: cargo nextest
        if: runner.os == 'Linux'
        run: cargo nextest run --workspace --locked --all-features --no-fail-fast

      - name: cargo nextest (non-Linux)
        if: runner.os != 'Linux'
        run: cargo nextest run --workspace ${{ env.XKB_LINUX_ONLY_EXCLUDE }} --locked --all-features --no-fail-fast

      - name: cargo test --doc
        if: runner.os == 'Linux'
        run: cargo test --doc --workspace --locked --all-features --no-fail-fast

      - name: cargo test --doc (non-Linux)
        if: runner.os != 'Linux'
        run: cargo test --doc --workspace ${{ env.XKB_LINUX_ONLY_EXCLUDE }} --locked --all-features --no-fail-fast

  test-stable-wasm:
    name: cargo test (wasm32)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install stable toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_STABLE_VER }}
          targets: wasm32-unknown-unknown

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      # TODO: Find a way to make tests work. Until then the tests are merely compiled.
      - name: cargo test compile
        run: cargo test --workspace ${{ env.XKB_LINUX_ONLY_EXCLUDE }} --locked --target wasm32-unknown-unknown --all-features --no-run

  check-msrv:
    name: cargo check (msrv)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v4

      - name: install msrv toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_MIN_VER }}
          targets: x86_64-unknown-none

      - name: install cargo-hack
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-hack

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: cargo check
        # The ui-events-wayland `xkb` feature pulls in xkbcommon, a third-party
        # binding to the system libxkbcommon that declares no MSRV, so it is
        # excluded here and not covered by this workspace's MSRV guarantee.
        run: cargo hack check ${{ env.RUST_MIN_VER_PKGS }} --locked --optional-deps --each-feature --ignore-unknown-features --exclude-features xkb --features std

  check-msrv-no-std:
    name: cargo check (msrv)
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    steps:
      - uses: actions/checkout@v4

      - name: install msrv toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_MIN_NO_STD_VER }}
          targets: x86_64-unknown-none

      - name: install cargo-hack
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-hack

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: cargo check (no_std)
        run: cargo hack check ${{ env.RUST_NO_STD_PKGS }} --locked --optional-deps --each-feature --exclude-features ${{ env.FEATURES_DEPENDING_ON_STD }} --features libm --target x86_64-unknown-none

  check-msrv-wasm:
    name: cargo check (msrv) (wasm32)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install msrv toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: ${{ env.RUST_MIN_VER }}
          targets: wasm32-unknown-unknown

      - name: install cargo-hack
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-hack

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: cargo check
        # `--exclude-features xkb` for the same reason as the non-wasm MSRV check:
        # the `xkb` feature links libxkbcommon (via `xkbcommon`), which neither
        # declares an MSRV nor builds for wasm.
        run: cargo hack check ${{ env.RUST_MIN_VER_PKGS }} --locked --target wasm32-unknown-unknown --optional-deps --each-feature --ignore-unknown-features --exclude-features xkb --features std

  miri:
    name: cargo miri
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install nightly toolchain
        uses: dtolnay/rust-toolchain@master
        with:
          toolchain: nightly
          components: miri
          targets: s390x-unknown-linux-gnu

      - name: install cargo-hack
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-hack

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      - name: cargo miri
        # The Wayland and X11 adapters' `xkb` feature links the system
        # libxkbcommon (C), which is unavailable to this cross-compiled job and
        # outside Miri's reach. Run the rest of the workspace with all features,
        # then those adapters (and the shared XKB crate) separately with their
        # default (xkb-free) features so their mapping and reducer tests still run
        # under Miri.
        run: cargo miri test --workspace ${{ env.XKB_LINUX_ONLY_EXCLUDE }} --locked --all-features --target s390x-unknown-linux-gnu --no-fail-fast

      - name: cargo miri (Linux/Unix adapters, xkb-free)
        run: cargo miri test -p ui-events-wayland -p ui-events-x11 -p ui-events-xkb --locked --target s390x-unknown-linux-gnu --no-fail-fast

  doc:
    name: cargo doc
    # NOTE: We don't have any platform specific docs in this workspace, so we only run on Ubuntu.
    #       If we get per-platform docs (win/macos/linux/wasm32/..) then doc jobs should match that.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: install nightly toolchain
        uses: dtolnay/rust-toolchain@nightly

      - name: restore cache
        uses: Swatinem/rust-cache@v2
        with:
          save-if: ${{ github.event_name != 'merge_group' }}

      # We test documentation using nightly to match docs.rs.
      - name: cargo doc
        run: cargo doc --workspace --locked --all-features --no-deps --document-private-items
        env:
          RUSTDOCFLAGS: '--cfg docsrs -D warnings'

  # If this fails, consider changing your text or adding something to .typos.toml.
  typos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: check typos
        uses: crate-ci/typos@v1.42.0
